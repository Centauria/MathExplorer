[project]
name = "mathx"
version = "0.1.0"
description = "AI math frontier explorer (omp-orchestrated, pluggable config.toml LLM fleet)"
requires-python = ">=3.14"
dependencies = ["httpx>=0.28"]

[tool.uv]
package = false
